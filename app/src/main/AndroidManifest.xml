<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chiararipanti.itranslate">

    <!-- Include required permissions for Google Mobile Ads to run -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >
        <activity
            android:name=".StartActivity"
            android:label="@string/app_name"
            android:theme="@style/AppBaseTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ImparaActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ImparaargomentiActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".TestoActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <activity
            android:name=".MusicaActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />

        <!-- Include the AdActivity configChanges and theme. -->
       <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
        <activity
            android:name=".ArgomentiActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".TraduzioneActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SpeechActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".QuizActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <activity
            android:name=".QuizLivelliActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".QuizTraduzioneActivity"
            android:label="@string/app_name" />
    </application>

</manifest>